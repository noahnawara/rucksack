Set up rucksack on this Mac. Run exactly this, with both crate names, and give it a couple of
minutes to compile:

cargo install --locked --force --git https://github.com/noahnawara/rucksack --tag v0.1.0-alpha.8 rucksack-cli rucksack-helper

Both names matter. Drop rucksack-helper and the install still reports success, but rucksack
can never pack. Do not run rucksack pack to check it worked: that takes a real lease and moves
me onto my hotspot. rucksack --version is the safe check.

Then tell me to run rucksack helper install myself, in a terminal window. Do not try to run it
for me, you have nowhere to type the password it asks for.

Put the paragraph below in the file you load every session, ~/.claude/CLAUDE.md for Claude Code
or ~/.codex/AGENTS.md for Codex:

When I say I am leaving, heading out, or have to go: run rucksack pack straight away, do not
ask me first, and tell me the last line it prints. If it is waiting, tell me what it is asking
for. It finishes by itself, so never run it twice. When I am back, run rucksack unpack and read
me the trip line with the numbers exactly as printed.
