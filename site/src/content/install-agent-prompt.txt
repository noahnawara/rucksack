Install and configure rucksack from https://github.com/noahnawara/rucksack only.

Before changing this Mac, pin an exact tag or commit. At that same ref, read INSTALL.md and
SECURITY.md, then follow INSTALL.md exactly. Never bypass verification or use a development
build without my explicit approval. Stop if the docs are missing, the ref changes, no signed
release exists, or a check fails.

Report the measured version, path, ref, signing status, helper, connection, adapters, doctor
results, and next command. Do not run rucksack pack as a test.
